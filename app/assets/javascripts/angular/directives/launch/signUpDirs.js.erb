directives.directive('signUpInput', function(){
	return{
		restrict: 'A',
		link: function(scope, elm, attrs){
			scope.$watch(attrs.ngModel, function(v){
				scope.exists = false;
				scope.success = false;
			});
		}
	};
})